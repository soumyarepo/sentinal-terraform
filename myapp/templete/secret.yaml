apiVersion: v1
items:
- apiVersion: v1
  data:
    privatekey.key: {{ .Files.Get (printf "certs/cconmis-api.%s.key" .Values.appEnv ) | b64enc }}
    certificate.crt: {{ .Files.Get (printf "certs/myapp.%s.crt" .Values.appEnv ) | b64enc }}
    certificate.pem: {{ .Files.Get ("certs/bundle_ca.pem") | b64enc}}
  kind: Secret
  metadata:
    name: {{ .Value.appname}}-cert
  type: Opaque
  apiVersion: v1
  data:
    
  